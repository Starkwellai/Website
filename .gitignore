node_modules/
dist/
.claude/
src/.claude/

# Pipeline output — regenerated by the data pipeline, not source. Shipped to
# the droplet via deploy/package_data.ps1 + scp, never via git (246MB and
# 110MB files here exceed GitHub's 100MB per-file limit anyway).
deploy/data/

# 79GB raw pricing pipeline workspace — lives on D:, not part of this repo.
utah_pricing/

# Figma export artifacts, not app source.
Onboarding Design.make
Onboarding Design.zip

*.log
.env
.env.local
.DS_Store
